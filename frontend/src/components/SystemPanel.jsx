import React, { useState, useEffect } from \"react\";\n\nexport default function SystemPanel() {\n  const [visionData, setVisionData] = useState({ people: 0, risk: \"UNKNOWN\", motion: \"IDLE\", pose: \"UNKNOWN\" });\n  const [audioData, setAudioData] = useState({ emotion: \"NEUTRAL\", confidence: 0, threat: \"NONE\" });\n\n  useEffect(() => {\n    fetch('/api/vision/status').then(r => r.json()).then(setVisionData).catch(e => console.error(e));\n    fetch('/api/audio/status').then(r => r.json()).then(setAudioData).catch(e => console.error(e));\n  }, []);\n\n  return (\n    <div className=\"system-panel\">\n      <div className=\"system-panel-header\">\n        <h2 className=\"system-panel-title\">System Status</h2>\n      </div>\n      <div className=\"system-panel-grid\">\n        <div className=\"system-panel-item\">\n          <div className=\"system-panel-item-header\">\n            <div className=\"system-panel-item-icon\">📹</div>\n            <h3 className=\"system-panel-item-title\">Vision System</h3>\n          </div>\n          <div className=\"system-panel-item-value\">{visionData.people}</div>\n          <p>People Detected</p>\n          <p><strong>Risk:</strong> {visionData.risk}</p>\n          <p><strong>Motion:</strong> {visionData.motion}</p>\n          <span className={`system-panel-item-status ${visionData.risk === 'LOW' ? 'active' : 'error'}`}>{visionData.risk}</span>\n        </div>\n        <div className=\"system-panel-item\">\n          <div className=\"system-panel-item-header\">\n            <div className=\"system-panel-item-icon\">🎤</div>\n            <h3 className=\"system-panel-item-title\">Audio System</h3>\n          </div>\n          <div className=\"system-panel-item-value\">{audioData.confidence}%</div>\n          <p>Confidence</p>\n          <p><strong>Emotion:</strong> {audioData.emotion}</p>\n          <p><strong>Threat:</strong> {audioData.threat}</p>\n          <span className={`system-panel-item-status ${audioData.threat === 'NONE' ? 'active' : 'error'}`}>{audioData.threat}</span>\n        </div>\n      </div>\n    </div>\n  );\n}
